# Reusable Benchmarks

Benchmark implementations belong here when they are useful to multiple
experiments.

Each benchmark directory should contain:

- `README.md`: purpose, prerequisites, and usage
- runnable scripts
- machine-readable output
- version and configuration capture
- cleanup instructions

Experiment conclusions should remain under `experiments/`.
